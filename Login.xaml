<Window x:Class="ProjetoEscola.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjetoEscola"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Login" Height="450" Width="350" WindowStartupLocation="CenterScreen">
    <Grid>
        <StackPanel Margin="20 60">
            <TextBlock Text="School System" HorizontalAlignment="Center" FontSize="30" Margin="0 0 0 20" />
            <TextBox 
                materialDesign:HintAssist.Hint="Usuário"
                materialDesign:TextFieldAssist.TextBoxViewMargin="1 0 1 0"
                Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                x:Name="txtEmail" Margin="10 10" FontSize="16" />

            <PasswordBox 
                materialDesign:HintAssist.Hint="Senha"
                Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                x:Name="passBoxSenha" Margin="10 10" FontSize="16" />

            <Button x:Name="BtnAcessar" Content="Acessar" Margin="10 20" Click="BtnAcessar_Click" />
        </StackPanel>
    </Grid>
</Window>
